.container
  .row
    h2
      | 注文履歴一覧
    table.table.table-hover.table-inverse
      thead
        tr
          th
            | 購入日時
          th
            | 購入者
          th
            | 注文個数
          th
            | 注文ステータス
      tbody
        tr
          - @orders.each do |order|
            td
              = link_to order.created_at, admin_order_path(order.id)
            td
              = order.member.last_name
              = order.member.first_name
            td
              = order.order_products.quantitiy
            td
              = order.status
